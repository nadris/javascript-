const calc = require('./index');

try {
    let sumTest = (calc.soma(2, 2));
    
    if (sumTest != 4) {
        throw new Error("Soma está errada: " + sumTest);
    }

    let subTest = (calc.subtracao(10,2));
    
    if (subTest != 8) {

        throw new Error("Subtração está errada: " + subTest);
    }
    console.log('**************** Passou testes  ****************')

} catch (e) {
    throw e.message;
}